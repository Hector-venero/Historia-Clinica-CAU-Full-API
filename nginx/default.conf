server {
    listen 80;
    server_name localhost;

    # AUMENTAR TAMAÑO DE SUBIDA (Vital para PDFs/Imágenes)
    client_max_body_size 20M;

    # 1. SERVIR ARCHIVOS ADJUNTOS (Directo desde disco, sin pasar por Flask)
    #    Esto busca en el volumen que montamos en /var/www/uploads
    location /uploads/ {
        alias /var/www/uploads/;
        autoindex off;        # Seguridad: No listar archivos si entran a la carpeta
        expires 30d;          # Cache: El navegador recuerda el archivo 30 días
        add_header Cache-Control "public, no-transform";
    }

    # 2. Todo lo que empiece con /api va al Backend (Flask)
    location /api/ {
        proxy_pass http://web:5000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 3. Todo lo demás va al contenedor del Frontend
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

##############################
# 🔒 BLOQUE HTTPS (ENTORNO PRODUCTIVO)
##############################
# ⚠️ Este bloque queda comentado hasta tener dominio y certificados SSL válidos.
# Una vez tengas el dominio, descomentá todo este bloque y reemplazá
# "historia-cau.unsam.edu.ar" por tu dominio real.

# server {
#     listen 443 ssl;
#     server_name historia-cau.unsam.edu.ar;

#     # 📜 Certificados SSL de Let's Encrypt (se crean con certbot)
#     ssl_certificate /etc/letsencrypt/live/historia-cau.unsam.edu.ar/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/historia-cau.unsam.edu.ar/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;

#     # 📁 Servir el frontend (build de React)
#     location / {
#         root /usr/share/nginx/html;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#     }

#     # 🔗 Proxy para backend Flask
#     location /api/ {
#         proxy_pass http://web:5000/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # 🧱 Seguridad avanzada
#     add_header X-Frame-Options "SAMEORIGIN";
#     add_header X-Content-Type-Options "nosniff";
#     add_header X-XSS-Protection "1; mode=block";
#     add_header Referrer-Policy "strict-origin-when-cross-origin";

#     # 🔄 Redirección HTTP->HTTPS automática
#     # (Certbot la agrega también automáticamente al instalar)
#     error_page 404 /index.html;
#     error_page 500 502 503 504 /index.html;
# }
