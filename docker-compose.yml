version: "3.9"

services:
  # ============================
  # 🐍 BACKEND (Flask)
  # ============================
  web:
    build: ./backend_flask/app
    container_name: historia_web
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./backend_flask/app:/app
      - ./backend_flask/app/static:/app/static
    depends_on:
      db:
        condition: service_healthy
      bfa-node:
        condition: service_started
    # 🔹 Cargar todas las variables desde el archivo .env
    env_file:
      - .env
    environment:
      - FLASK_ENV=${FLASK_ENV}
      - FLASK_DEBUG=${FLASK_DEBUG}
      - FLASK_APP=main.py
      - TZ=America/Argentina/Buenos_Aires
    # 🔹 Usa script de inicio que detecta automáticamente el modo (dev/prod)
    command: bash -c "echo Starting Flask with ENV=$FLASK_ENV && /wait-for-it.sh db:3306 -- /app/start.sh"
    networks:
      - historia_net
      
  # ============================
  # 🗄️ BASE DE DATOS (MySQL)
  # ============================
  db:
    image: mysql:8.0
    container_name: historia_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hc_bfa
      TZ: America/Argentina/Buenos_Aires
    command: --default-time-zone='America/Argentina/Buenos_Aires'
    volumes:
      - db_data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    # 🔹 Descomentá si necesitás acceso externo (ej. MySQL Workbench)
    # ports:
    #   - "3307:3306"
    networks:
      - historia_net
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -proot || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10

  # ============================
  # ⛓️ NODO BLOCKCHAIN (BFA / Geth)
  # ============================
  bfa-node:
    image: ethereum/client-go:stable
    container_name: bfa-node
    restart: always
    entrypoint: ["geth"]
    command: >
      --dev
      --http --http.addr 0.0.0.0 --http.vhosts="*" --http.corsdomain="*"
      --http.api admin,eth,net,web3,miner,personal,txpool
      --datadir /nucleo/test2network
      --rpc.allow-unprotected-txs
      --verbosity 3
    ports:
      - "8545:8545"
      - "30303:30303"
    volumes:
      - ./bfa-node/nucleo:/nucleo
      - bfa_data:/nucleo/test2network/geth
    environment:
      - TZ=America/Argentina/Buenos_Aires
    networks:
      - historia_net

  # ============================
  # 💻 FRONTEND (React / Vite)
  # ============================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: historia_frontend
    restart: always
    env_file:
      - .env
    environment:
      #- VITE_API_URL=${API_URL}
       - VITE_API_URL=${VITE_API_URL}
    volumes:
      - ./frontend:/app
    depends_on:
      - web
    networks:
      - historia_net
    #  No exponer el puerto 5173 en producción (solo se usa para desarrollo)
    ports:
      - "5173:5173"

  # ============================
  # 🌍 REVERSE PROXY (NGINX)
  # ============================
  nginx:
    image: nginx:latest
    container_name: historia_nginx
    restart: always
    depends_on:
      - web
      - frontend
    ports:
      - "80:80"
      # - "443:443"  # ✅ Descomentar al habilitar HTTPS
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./frontend/dist:/usr/share/nginx/html
      # - /etc/letsencrypt:/etc/letsencrypt  # ✅ Descomentar cuando tengas certificados SSL
    networks:
      - historia_net

# ============================
# 🌐 REDES Y VOLÚMENES
# ============================
networks:
  historia_net:

volumes:
  db_data:
  bfa_data:
